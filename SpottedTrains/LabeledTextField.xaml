<UserControl x:Class="SpottedTrains.LabeledTextField"
         xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
         xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
         xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
         xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
         mc:Ignorable="d" 
         d:DesignHeight="30" d:DesignWidth="340">
    <UserControl.Resources>
    </UserControl.Resources>
    <Grid x:Name="Root">
        <Label Content="There is the textbox"/>
        <TextBox Width="100"
                 Height="20"
                 Text="{Binding Value, NotifyOnValidationError=True, UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True}"/>
    </Grid>
</UserControl>
    